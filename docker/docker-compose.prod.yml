version: '3.7'

services:
  app:
    image: fastapi-test
    container_name: fastapi-test
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod
    ports:
      - "8000:8000"
    volumes:
      - ../data:/data
