version: '3.7'

services:
  app:
    image: sweaty-reports-dev
    container_name: sweaty-reports-dev
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - ..:/app
      - ../data:/data
